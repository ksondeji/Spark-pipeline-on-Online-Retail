# Dépendances communes (local + Databricks si installées sur le cluster)
python-dotenv>=1.0.0
pyyaml>=6.0.2

# Dépendances pour exécution LOCALE uniquement (pip install -r requirements-local.txt)
# Sur Databricks : PySpark et Delta sont fournis par le runtime — ne pas les réinstaller.
